/**
 * Kept in step with the version in package.json. The User-Agent embeds it, so
 * Pequerecetas can tell which build is calling from its own logs.
 */
export const PKG_VERSION = "1.0.1";
export const REPO_URL = "https://github.com/smeet666/mcp-pequerecetas";
